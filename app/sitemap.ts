import type { MetadataRoute } from "next";
import { tools } from "./lib/tools";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://taxmathkit.com"; const staticPaths = ["", "/guides", "/methodology", "/privacy", "/terms", "/disclaimer"]; return [...staticPaths.map((path) => ({ url: `${base}${path}`, lastModified: new Date("2026-08-05"), changeFrequency: path ? "monthly" as const : "weekly" as const, priority: path ? 0.6 : 1 })), ...tools.map((tool) => ({ url: `${base}/tools/${tool.slug}`, lastModified: new Date("2026-08-05"), changeFrequency: "monthly" as const, priority: 0.9 }))]; }
